import React from "react";
import "./Header.css";
function Header() {
  return (
    <nav className="header">
      <button>Start New Game</button>
      <button>Back</button>
    </nav>
  );
}

export default Header;
